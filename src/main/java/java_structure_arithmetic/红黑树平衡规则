红黑书的特征：
    1. 节点都有颜色
    2. 在插入和删除的过程中，要遵循保持这些颜色的不同排列的规则

红黑规则：
    1. 每一个节点不是红的就是黑的
    2. 根总是黑的
    3. 如果节点是红色的，则它的子节点必须是黑色的（反之不一定是真的）
    4. 从根到叶节点或空子节点的每条路径，必须包含相同数目的黑色节点
        （从根到叶节点路径上的黑色节点的数目称为黑色高度）
      所有从跟到叶节点的路径上的黑色高度必须相同
重复的关键字
    如果关键字的序列为50，50，50，要把第二个50放到第一个50的右边，并且把第三个50放到第一个50的左边
